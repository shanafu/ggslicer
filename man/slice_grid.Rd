% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{slice_grid}
\alias{slice_grid}
\title{Build a box + gridline overlay for a slice (or slices)}
\usage{
slice_grid(
  image = NULL,
  axis = NULL,
  coordinate = NULL,
  spacing = NULL,
  point_spacing = NULL,
  padding = 0,
  geometry = NULL
)
}
\arguments{
\item{image}{A \code{SimpleITK} image, or a file path (read internally via
\code{\link[=ReadImage_fix]{ReadImage_fix()}}). Required unless \code{geometry} is supplied.}

\item{axis}{Which image axis is out-of-plane (see \code{\link[=build_slice_geometry]{build_slice_geometry()}}).
Required unless \code{geometry} is supplied; must not be supplied together
with \code{geometry}.}

\item{coordinate}{One or more world coordinates along \code{axis} (see
\code{\link[=build_slice_geometry]{build_slice_geometry()}}). Required unless \code{geometry} is supplied; must
not be supplied together with \code{geometry}.}

\item{spacing}{Distance between adjacent gridlines, the same for both
horizontal and vertical. Defaults (if \code{NULL}) to \code{min(extent) / 10},
scale-adaptive so it's reasonable whether the slice is measured in mm
or µm.}

\item{point_spacing}{Distance between points along a single line,
controlling how smooth/coarse it looks when plotted. Defaults (if
\code{NULL}) to \code{spacing / 20}.}

\item{padding}{Extend (positive) or inset (negative) the box beyond the
slice's true extent. Default \code{0} (exact bounds).}

\item{geometry}{A pre-built \link{SliceGeometry}/\link{SlicePackage}/\link{SlicePackageSet},
used instead of \code{axis}/\code{coordinate}. Must not be supplied together with
\code{axis}/\code{coordinate}.}
}
\value{
A tibble with columns \code{package}, \code{k}, \code{part} (\code{"box"} or
\code{"grid"}), \code{grid_axis} (\code{"i"} or \code{"j"}: which in-plane axis is held
fixed along this line), \code{line_id} (distinguishes different lines
sharing the same \code{part}/\code{grid_axis}), \code{vertex} (1-based order within
the line), \code{x}, \code{y}, \code{z} (world coordinates).
}
\description{
Computes an overlay grid for each slice in the requested geometry: an
outer bounding box plus evenly-spaced horizontal and vertical lines
(same spacing in both directions), each line a densely-sampled ordered
set of points suitable for \code{ggplot2::geom_path()}. Typical uses: a visual
reference grid over an anatomical slice, or (combined with
\code{\link[=transform_points]{transform_points()}}) a grid warped through a registration transform, to
visualize what that transform does to the image.

Unlike \code{\link[=slice_image]{slice_image()}}/\code{\link[=slice_contours]{slice_contours()}}, no image is sampled here — the
grid is pure geometry, computed entirely from the resolved
\link{SliceGeometry}/\link{SlicePackage}/\link{SlicePackageSet}. \code{image} (with
\code{axis}/\code{coordinate}) exists only for interface consistency with those
functions, feeding \code{\link[=build_slice_geometry]{build_slice_geometry()}} when \code{geometry} isn't
supplied directly.
}
\examples{
\dontrun{
image <- ReadImage_fix("brain_image.nii")
grid_df <- slice_grid(image, axis = "axial", coordinate = 0, spacing = 5)
}
}
