% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation.R
\name{slice_label_annotations_layer}
\alias{slice_label_annotations_layer}
\title{Ready-made ggplot2 layer for region-name annotations produced by slice_label_annotations()}
\usage{
slice_label_annotations_layer(
  annotations_df,
  color = "black",
  size = 3.5,
  alpha = 1,
  fontface = "plain"
)
}
\arguments{
\item{annotations_df}{A data frame as returned by
\code{\link[=slice_label_annotations]{slice_label_annotations()}}/\code{\link[=contour_centroids]{contour_centroids()}}.}

\item{color, size, alpha, fontface}{Style for the text labels, passed to
\code{ggplot2::geom_text()}.}
}
\value{
A single \code{ggplot2::geom_text()} layer. Add it (e.g. \code{plt + layer})
to a plot that already maps \code{x}/\code{y} via its own \code{aes()}.
}
\description{
Wraps \code{ggplot2::geom_text()} for a \code{\link[=slice_label_annotations]{slice_label_annotations()}}/
\code{\link[=contour_centroids]{contour_centroids()}} data frame, so it can be added to a plot directly --
the same ready-made-layer convenience \code{slice_grid_layers()}/
\code{slice_warp_arrows_layer()} provide for their own outputs.
}
\examples{
\dontrun{
atlas <- ReadImage_fix("atlas_labels.nii.gz")
annotations <- slice_label_annotations(atlas, axis = "axial", coordinate = 0)
layer <- slice_label_annotations_layer(annotations)

library(ggplot2)
ggplot(annotations, aes(x = x, y = y)) + layer
}
}
