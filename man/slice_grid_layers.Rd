% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{slice_grid_layers}
\alias{slice_grid_layers}
\title{Ready-made ggplot2 layers for a grid produced by slice_grid()}
\usage{
slice_grid_layers(
  grid_df,
  box_color = "orange",
  box_linewidth = 1,
  box_alpha = 1,
  grid_color = "#FF6666",
  grid_linewidth = 0.25,
  grid_alpha = 0.8
)
}
\arguments{
\item{grid_df}{A data frame as returned by \code{\link[=slice_grid]{slice_grid()}}.}

\item{box_color, box_linewidth, box_alpha}{Style for the box layer.}

\item{grid_color, grid_linewidth, grid_alpha}{Style for the grid layer.}
}
\value{
A named list with elements \code{box} and \code{grid}, each a
\code{ggplot2::geom_path()} layer. Add both (e.g. \code{plt + layers$box + layers$grid})
to a plot that already maps \code{x}/\code{y} via its own \code{aes()}.
}
\description{
\code{slice_grid()}'s box and grid lines are two visually-distinct components
that normally need two separate \code{geom_path()} calls with different
styling. This returns both as ready-made, composable layers to \code{+} onto
a plot, rather than requiring the user to filter \code{part} and write the
grouping expression themselves.
}
