% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{transform_points}
\alias{transform_points}
\title{Apply a SimpleITK transform to a tidy data frame's world coordinates}
\usage{
transform_points(
  df,
  transform,
  invert = FALSE,
  x_col = "x",
  y_col = "y",
  z_col = "z"
)
}
\arguments{
\item{df}{A data frame with \code{x_col}/\code{y_col}/\code{z_col} columns.}

\item{transform}{A \code{SimpleITK} transform object, or a file path (read via
\code{\link[=read_minc_transform]{read_minc_transform()}} for \code{.xfm}, or \code{SimpleITK::ReadTransform()}
otherwise).}

\item{invert}{If \code{TRUE}, apply the inverse of \code{transform}
(\code{transform$GetInverse()}). Works for affine/linear transforms; SimpleITK
does not support inverting a \code{DisplacementFieldTransform} (or a
composite containing one) this way and will error clearly if asked —
load the separately-computed inverse-warp file instead (the standard
registration-tool convention, and why e.g. ANTs always writes both
\verb{*Warp.nii.gz} and \verb{*InverseWarp.nii.gz}).}

\item{x_col, y_col, z_col}{Names of the world-coordinate columns to
transform.}
}
\value{
\code{df}, with \code{x_col}/\code{y_col}/\code{z_col} replaced by their transformed
coordinates.
}
\description{
A small, generic utility: given any tidy data frame with \code{x}/\code{y}/\code{z}
(world coordinate) columns — \code{\link[=slice_grid]{slice_grid()}}'s output, \code{\link[=slice_contours]{slice_contours()}}'s
output, or anything else — replaces those columns with their positions
after applying \code{transform}, leaving every other column untouched. This is
what makes visualizing a registration warp possible: build a grid (or
contour, or any point set) on the fixed/reference image, then transform
its points through the registration transform.
}
