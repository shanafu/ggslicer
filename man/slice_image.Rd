% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{slice_image}
\alias{slice_image}
\title{Sample a slice (or slices) of an image, ready for grammar-of-graphics plotting}
\usage{
slice_image(
  image,
  axis = NULL,
  coordinate = NULL,
  extra_images = list(),
  interpolator = "sitkLinear",
  interpolator_overrides = list(),
  discrete_names = discrete_data_names(),
  extra_index = list(),
  geometry = NULL
)
}
\arguments{
\item{image}{A \code{SimpleITK} image, or a file path (read internally via
\code{\link[=ReadImage_fix]{ReadImage_fix()}}).}

\item{axis}{Which image axis is out-of-plane (see \code{\link[=build_slice_geometry]{build_slice_geometry()}}).
Required unless \code{geometry} is supplied; must not be supplied together
with \code{geometry}.}

\item{coordinate}{One or more world coordinates along \code{axis} (see
\code{\link[=build_slice_geometry]{build_slice_geometry()}}). Required unless \code{geometry} is supplied; must
not be supplied together with \code{geometry}.}

\item{extra_images}{A fully named list of additional images/paths to
sample onto the same geometry (e.g. \code{list(mask = "brainmask.nii.gz")}),
sampled into columns named after the list.}

\item{interpolator}{Default SimpleITK interpolator name for \code{image} and
any \code{extra_images} not otherwise matched; see \code{\link[=sample_images]{sample_images()}}.}

\item{interpolator_overrides}{A named list of explicit per-image
interpolator overrides; see \code{\link[=sample_images]{sample_images()}}.}

\item{discrete_names}{Character vector of names treated as discrete
(categorical) data; see \code{\link[=discrete_data_names]{discrete_data_names()}}.}

\item{extra_index}{Non-spatial index selection for 4D/5D images; see
\code{\link[=sample_images]{sample_images()}}.}

\item{geometry}{A pre-built \link{SliceGeometry}/\link{SlicePackage}/\link{SlicePackageSet},
used instead of \code{axis}/\code{coordinate} (e.g. for an oblique DICOM-derived
geometry). Must not be supplied together with \code{axis}/\code{coordinate}.}
}
\value{
A tibble; see \code{\link[=sample_images]{sample_images()}}.
}
\description{
The main user-facing entry point: given an image, an axis, and one or more
world coordinates along it (or, via the \code{geometry} escape hatch, a
hand-built \link{SliceGeometry}/\link{SlicePackage}/\link{SlicePackageSet} for oblique or
custom cases), returns a tidy data frame with one row per sample point and
one column per image (the main \code{image}, named \code{"value"}, plus any
\code{extra_images}) — ready to hand to \code{ggplot2}.

Internally, this is \code{\link[=build_slice_geometry]{build_slice_geometry()}} (unless \code{geometry} is
supplied directly) followed by \code{\link[=sample_images]{sample_images()}}; see those for the
geometry-construction and interpolator-selection details.
}
