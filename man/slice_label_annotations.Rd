% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation.R
\name{slice_label_annotations}
\alias{slice_label_annotations}
\title{Place region-name text at the centroid of each labeled region in a slice}
\usage{
slice_label_annotations(
  image,
  axis = NULL,
  coordinate = NULL,
  labels = NULL,
  label_names = NULL,
  mask = NULL,
  mask_fill = c("zero", "nan"),
  min_vertices = NULL,
  min_area = NULL,
  geometry = NULL
)
}
\arguments{
\item{image}{A \code{SimpleITK} image, or a file path (read internally via
\code{\link[=ReadImage_fix]{ReadImage_fix()}}), containing integer-valued (or integer-valued-once-
rounded) label data. Always sampled nearest-neighbor, since label data
is discrete.}

\item{axis}{Which image axis is out-of-plane (see \code{\link[=build_slice_geometry]{build_slice_geometry()}}).
Required unless \code{geometry} is supplied; must not be supplied together
with \code{geometry}.}

\item{coordinate}{One or more world coordinates along \code{axis} (see
\code{\link[=build_slice_geometry]{build_slice_geometry()}}). Required unless \code{geometry} is supplied; must
not be supplied together with \code{geometry}.}

\item{labels}{Optional numeric vector restricting which label values are
contoured. Defaults to every nonzero label value present in each slice.}

\item{label_names}{Optional named vector mapping a label's integer value
(as a string name) to its display text; see \code{\link[=contour_centroids]{contour_centroids()}}.}

\item{mask}{An optional \code{SimpleITK} image or path, sampled
nearest-neighbor onto the same geometry and applied per \code{mask_fill}
before contouring. Since \code{0} is already the "no label" sentinel that's
always excluded from contouring, \code{mask_fill} makes no practical
difference here (unlike \code{\link[=slice_contours]{slice_contours()}}) — it's offered for
consistency with that function's interface, not because the two modes
diverge for label data.}

\item{mask_fill}{See \code{\link[=slice_contours]{slice_contours()}}; \code{"zero"} (default) or \code{"nan"}.}

\item{min_vertices}{Optional single integer: drop any individual contour
path (a \code{package}/\code{k}/\code{label}/\code{obj} group) with fewer than this many
vertices.}

\item{min_area}{Optional single number: drop any connected component
whose true polygon area is below this value; see \code{\link[=contour_centroids]{contour_centroids()}}.
Independent of \code{min_vertices}, which is \code{\link[=slice_label_contours]{slice_label_contours()}}'s own
perimeter/vertex-count-based filter.}

\item{geometry}{A pre-built \link{SliceGeometry}/\link{SlicePackage}/\link{SlicePackageSet},
used instead of \code{axis}/\code{coordinate}. Must not be supplied together with
\code{axis}/\code{coordinate}.}
}
\value{
A tibble; see \code{\link[=contour_centroids]{contour_centroids()}}.
}
\description{
A one-call convenience wrapper: runs \code{\link[=slice_label_contours]{slice_label_contours()}} then
\code{\link[=contour_centroids]{contour_centroids()}} on the result -- for placing region-name text
annotations directly from an image/axis/coordinate, without first
building the contours data frame yourself. If you already need
\code{slice_label_contours()}'s output separately (e.g. to also draw region
outlines via \code{geom_path()}), call \code{\link[=contour_centroids]{contour_centroids()}} directly on that
existing result instead, to avoid recomputing the same contours twice.
}
\examples{
\dontrun{
atlas <- ReadImage_fix("atlas_labels.nii.gz")
annotations <- slice_label_annotations(
  atlas, axis = "axial", coordinate = 0,
  label_names = c("187" = "Hippocampus", "10557" = "Cortex"),
  min_area = 2
)

library(ggplot2)
ggplot(annotations, aes(x = x, y = y, label = name)) + geom_text()
}
}
