% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warpfield.R
\name{slice_warp_arrows_layer}
\alias{slice_warp_arrows_layer}
\title{Ready-made ggplot2 layer for a warp-arrow overlay produced by slice_warp_arrows()}
\usage{
slice_warp_arrows_layer(
  arrows_df,
  color = "#2C7FB8",
  linewidth = 0.5,
  alpha = 1,
  arrow_length_inches = 0.08,
  arrow_type = c("open", "closed")
)
}
\arguments{
\item{arrows_df}{A data frame as returned by \code{\link[=slice_warp_arrows]{slice_warp_arrows()}}.}

\item{color, linewidth, alpha}{Style for the arrow segments.}

\item{arrow_length_inches}{Length of the drawn arrowhead itself (inches,
via \code{grid::arrow()}'s \code{length}) -- unrelated to \code{slice_warp_arrows()}'s
own \code{arrow_length} argument, which controls the \emph{shaft}'s world-space
length, not the arrowhead's on-page size.}

\item{arrow_type}{\code{"open"} (default) or \code{"closed"}, passed to \code{grid::arrow()}.}
}
\value{
A single \code{ggplot2::geom_segment()} layer, with an arrowhead. Add it
(e.g. \code{plt + layer}) to a plot that already maps \code{x}/\code{y} via its own \code{aes()}.
}
\description{
Wraps \code{ggplot2::geom_segment()} with an arrowhead (\code{grid::arrow()}), so a
\code{slice_warp_arrows()} data frame can be added to a plot directly, without
the caller having to remember the \verb{arrow =} argument -- the same
ready-made-layer convenience \code{slice_grid_layers()} provides for \code{slice_grid()}.
}
\examples{
\dontrun{
fixed_image <- ReadImage_fix("fixed_template.nii")
arrows <- slice_warp_arrows(
  fixed_image, axis = "axial", coordinate = 0,
  warp = "registration.xfm"
)
layer <- slice_warp_arrows_layer(arrows)

library(ggplot2)
ggplot(arrows, aes(x = x, y = y, xend = xend, yend = yend)) + layer
}
}
