% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{discrete_data_names}
\alias{discrete_data_names}
\title{Names that mark an image as discrete (categorical) data}
\usage{
discrete_data_names()
}
\value{
A character vector of names.
}
\description{
The default set of image names that \code{\link[=sample_images]{sample_images()}}/\code{\link[=slice_image]{slice_image()}}
treat as discrete/categorical data (masks, labels, atlases) rather than
continuous intensity data, and therefore always sample with
\code{"sitkNearestNeighbor"} regardless of the \code{interpolator} requested for
everything else. Matching is by whole token (splitting the image's name
on runs of non-alphanumeric characters), case-insensitively — so
\code{"brain_mask"} matches (token \code{"mask"}) but \code{"landmasking_score"} does not
(no token equals a discrete name exactly).
}
\examples{
\dontrun{
discrete_data_names()
}
}
