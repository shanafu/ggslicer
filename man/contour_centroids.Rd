% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation.R
\name{contour_centroids}
\alias{contour_centroids}
\title{Compute one label per connected component from a slice_label_contours() data frame}
\usage{
contour_centroids(contours_df, label_names = NULL, min_area = NULL)
}
\arguments{
\item{contours_df}{A data frame as returned by \code{\link[=slice_label_contours]{slice_label_contours()}}
(columns \code{package}, \code{k}, \code{label}, \code{obj}, \code{vertex}, \code{x}, \code{y}, \code{z}).}

\item{label_names}{Optional named vector mapping a label's integer value
(as a string name, e.g. \code{c("187" = "Hippocampus")}) to its display
text. Labels not present in \code{label_names} (or when \code{label_names} is
\code{NULL}) fall back to their own integer value as a string.}

\item{min_area}{Optional single number: drop any connected component
whose true polygon area (world-space units², e.g. mm^2) is below this
value -- a more accurate size filter than
\code{\link[=slice_label_contours]{slice_label_contours()}}'s own \code{min_vertices} (which really tracks
perimeter/boundary complexity, not enclosed area).}
}
\value{
A tibble with columns \code{package}, \code{k}, \code{label}, \code{name} (display
text), \code{obj}, \code{x}, \code{y}, \code{z} (centroid world coordinates), \code{area}
(world-space units²).
}
\description{
Given the output of \code{\link[=slice_label_contours]{slice_label_contours()}}, finds the centroid of each
connected component (each \code{package}/\code{k}/\code{label}/\code{obj} group already
distinguishes separate components sharing the same label -- a label
present as multiple disjoint regions in one slice gets one row per
region here, not one row per label) -- ready for
\code{ggplot2::geom_text(aes(x = x, y = y, label = name))} as region-name
annotations.

The centroid is the true, area-weighted centroid of the enclosed polygon
(a triangle-fan computation, done directly in world coordinates), not
just the mean of the traced boundary's vertices -- more robust for
irregular/concave region shapes,
though even this can occasionally fall outside a very non-convex (e.g.
crescent-shaped) region; inspect placements before relying on them for a
specific figure.
}
\examples{
\dontrun{
atlas <- ReadImage_fix("atlas_labels.nii.gz")
contours <- slice_label_contours(atlas, axis = "axial", coordinate = 0)
annotations <- contour_centroids(
  contours,
  label_names = c("187" = "Hippocampus", "10557" = "Cortex"),
  min_area = 2
)

library(ggplot2)
ggplot(annotations, aes(x = x, y = y, label = name)) + geom_text()
}
}
