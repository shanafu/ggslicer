% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{ReadImage_fix}
\alias{ReadImage_fix}
\title{Read an image file, correcting MINC orientation for display}
\usage{
ReadImage_fix(file)
}
\arguments{
\item{file}{Path to an image file.}
}
\value{
A \code{SimpleITK} image.
}
\description{
Reads \code{file} via \code{SimpleITK::ReadImage()}. If \code{file} is a MINC file
(\code{.mnc}/\code{.minc}), also applies \code{\link[=orientation_correction]{orientation_correction()}} and records the
original MINC direction matrix and file type as image metadata
(\code{OriginalFileType}, \code{OriginalDirection}), so \code{\link[=WriteImage_fix]{WriteImage_fix()}} can later
undo the correction if writing back out to MINC or another format.
}
\examples{
\dontrun{
# A MINC file gets its orientation corrected for display; a NIfTI file is
# returned unchanged (still tagged with metadata for WriteImage_fix()).
image <- ReadImage_fix("brain_image.mnc")
image2 <- ReadImage_fix("brain_image.nii")
}
}
