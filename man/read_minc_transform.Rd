% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{read_minc_transform}
\alias{read_minc_transform}
\title{Read a MINC transform (\code{.xfm}) file as a SimpleITK transform}
\usage{
read_minc_transform(path)
}
\arguments{
\item{path}{Path to a \code{.xfm} file.}
}
\value{
A \code{SimpleITK} transform: a single \code{AffineTransform} or
\code{DisplacementFieldTransform} if \code{path} has exactly one block, or a
\code{CompositeTransform} (applying the blocks in file order) if it has more
than one.
}
\description{
Parses an MNI transform file directly (no dependency beyond what this
package already requires): one or more \code{Transform_Type = Linear;} blocks
(a 3x4 matrix) and/or \code{Transform_Type = Grid_Transform;} blocks (a
reference to a companion displacement-field MINC volume, resolved
relative to \code{path}'s own directory), optionally concatenated in one file.
An \code{Invert_Flag} on a \code{Grid_Transform} block is ignored — confirmed
(round-tripping a real forward/inverse transform pair) that the
referenced displacement volume already contains the correct-direction
field, needing no extra sign handling.

\code{SimpleITK::ReadTransform()} must not be used for a \code{.xfm} file containing
a \code{Grid_Transform} block or multiple concatenated blocks — confirmed it
silently returns a transform with the wrong dimensions and all-zero
displacement rather than erroring. (A pure single-\code{Linear}-block \code{.xfm}
does parse correctly via \code{SimpleITK::ReadTransform()}, but this function
handles that case too, for a single entry point regardless of content.)
}
