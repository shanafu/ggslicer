% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{build_slice_geometry}
\alias{build_slice_geometry}
\title{Build a slice geometry from an image and world coordinates along one axis}
\usage{
build_slice_geometry(image, axis, coordinate)
}
\arguments{
\item{image}{A 3D \code{SimpleITK} image.}

\item{axis}{Which image axis is out-of-plane: \code{1}/\code{2}/\code{3}, \code{"x"}/\code{"y"}/\code{"z"},
or (assuming right-anterior-superior orientation)
\code{"sagittal"}/\code{"coronal"}/\code{"axial"}/\code{"horizontal"}.}

\item{coordinate}{A numeric vector of one or more world coordinates along
\code{axis} (each snapped to the nearest voxel plane, as in
\code{SliceGeometry$from_image_axis()}). Need not be evenly spaced.}
}
\value{
A new \link{SlicePackageSet}.
}
\description{
The "smart" geometry builder behind \code{\link[=slice_image]{slice_image()}}'s default (non-escape-
hatch) path: given an image, an out-of-plane axis, and one or more world
coordinates along that axis, builds the corresponding axis-aligned
slice(s) (via \link[=SliceGeometry]{SliceGeometry$from_image_axis()}) and always
returns a \link{SlicePackageSet}, for a uniform return type regardless of input:
\itemize{
\item A single \code{coordinate} gives a \code{SlicePackageSet} with one single-slice package.
\item Multiple, evenly-spaced coordinates give a \code{SlicePackageSet} with one
regular \link{SlicePackage} (so the whole stack resamples in a single call).
\item Multiple, unevenly-spaced coordinates give a \code{SlicePackageSet} with one
single-slice package per coordinate (since a \link{SlicePackage} cannot
represent uneven spacing) — auto-detected, not something the caller
needs to specify.
}
}
