% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{sample_images}
\alias{sample_images}
\title{Sample one or more SimpleITK images onto a shared slice geometry}
\usage{
sample_images(
  geometry,
  images,
  interpolator = "sitkLinear",
  interpolator_overrides = list(),
  discrete_names = discrete_data_names(),
  extra_index = list()
)
}
\arguments{
\item{geometry}{A \link{SliceGeometry}, \link{SlicePackage}, or \link{SlicePackageSet} object.}

\item{images}{A single \code{SimpleITK} image or file path (sampled into an
\code{"intensity"} column), or a fully named list of images/paths (sampled
into columns named after the list).}

\item{interpolator}{Default SimpleITK interpolator name (e.g.
\code{"sitkLinear"}, the default, or \code{"sitkNearestNeighbor"}), used for any
image not matched by \code{discrete_names} or \code{interpolator_overrides}.}

\item{interpolator_overrides}{A named list mapping an image's name to an
explicit interpolator, taking precedence over both \code{interpolator} and
the \code{discrete_names} rule.}

\item{discrete_names}{Character vector of names (matched by whole token,
case-insensitively) that always use \code{"sitkNearestNeighbor"}. Defaults to
\code{\link[=discrete_data_names]{discrete_data_names()}}.}

\item{extra_index}{A named list of 0-indexed index vectors, one per
non-spatial dimension shared by any higher-dimensional (4D/5D) image in
\code{images} (e.g. \code{list(t = 0:9)}). Images that are exactly 3D ignore this
and are sampled once, broadcasting across every combination.}
}
\value{
A tibble with columns \code{package}, any names in \code{extra_index}, \code{i},
\code{j}, \code{k}, \code{x}, \code{y}, \code{z}, and one column per name in \code{images}.
}
\description{
The core sampling primitive behind \code{\link[=build_slice_geometry]{build_slice_geometry()}}/\code{\link[=slice_image]{slice_image()}}:
given a geometry (any of \link{SliceGeometry}, \link{SlicePackage}, or
\link{SlicePackageSet} — auto-coerced to a \code{SlicePackageSet}) and one or more
named images, returns a single tidy data frame with one row per sample
point and one column per image.

Each image may be given as an already-loaded \code{SimpleITK} image or as a
file path (read internally via \code{\link[=ReadImage_fix]{ReadImage_fix()}}). The interpolator used
for each image is \code{interpolator} by default, except that images whose
name matches \code{\link[=discrete_data_names]{discrete_data_names()}} (e.g. \code{"mask"}, \code{"label"}, \code{"atlas"})
always use \code{"sitkNearestNeighbor"}, and \code{interpolator_overrides} always
wins over both. Images with more than 3 dimensions are indexed via
\code{extra_index}; images that are exactly 3D ignore \code{extra_index} and have
their (single) sampled value broadcast across every combination present
from higher-dimensional images.
}
