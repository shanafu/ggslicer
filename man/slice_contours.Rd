% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contours.R
\name{slice_contours}
\alias{slice_contours}
\title{Extract iso-intensity contour paths from a slice (or slices) of an image}
\usage{
slice_contours(
  image,
  axis = NULL,
  coordinate = NULL,
  levels,
  mask = NULL,
  mask_fill = c("zero", "nan"),
  min_vertices = NULL,
  geometry = NULL
)
}
\arguments{
\item{image}{A \code{SimpleITK} image, or a file path (read internally via
\code{\link[=ReadImage_fix]{ReadImage_fix()}}).}

\item{axis}{Which image axis is out-of-plane (see \code{\link[=build_slice_geometry]{build_slice_geometry()}}).
Required unless \code{geometry} is supplied; must not be supplied together
with \code{geometry}.}

\item{coordinate}{One or more world coordinates along \code{axis} (see
\code{\link[=build_slice_geometry]{build_slice_geometry()}}). Required unless \code{geometry} is supplied; must
not be supplied together with \code{geometry}.}

\item{levels}{Numeric vector of intensity levels to contour.}

\item{mask}{An optional \code{SimpleITK} image or path, sampled
nearest-neighbor onto the same geometry and applied per \code{mask_fill}
before contouring.}

\item{mask_fill}{How to treat voxels excluded by \code{mask} (values \verb{< 0.5}):
\code{"zero"} (default) zero-fills them, which — since 0 is a value that can
itself be a requested contour level — also traces the mask's own edge
as a contour (useful for drawing a mask/brain outline); \code{"nan"} fills
them with \code{NaN} instead, which \code{contourLines()} skips over without
fabricating a boundary there, better suited to continuous data (e.g. a
signed statistical map) where 0 is itself a meaningful in-range value.}

\item{min_vertices}{Optional single integer: drop any individual contour
path (a \code{package}/\code{k}/\code{level}/\code{obj} group) with fewer than this many
vertices, to remove small/noisy paths.}

\item{geometry}{A pre-built \link{SliceGeometry}/\link{SlicePackage}/\link{SlicePackageSet},
used instead of \code{axis}/\code{coordinate}. Must not be supplied together with
\code{axis}/\code{coordinate}.}
}
\value{
A tibble with columns \code{package}, \code{k}, \code{level}, \code{obj} (contour-path
ID within that package/slice/level), \code{vertex} (1-based order within the
path — always present and pre-sorted, unlike the row-order-dependent
legacy output), \code{x}, \code{y}, \code{z} (world coordinates).
}
\description{
The contour-path counterpart to \code{\link[=slice_image]{slice_image()}}: instead of per-voxel
intensities, returns the vertices of iso-intensity contour lines (via
\code{grDevices::contourLines()}, i.e. marching squares) at one or more
requested \code{levels}, for one or more slices — ready for \code{ggplot2::geom_path()}
rather than a filled raster. Suitable for overlaying a statistical-map
threshold or any other iso-intensity boundary on a continuous image.

Contouring happens in each slice's own local in-plane coordinates and is
then mapped to world coordinates via that slice's \code{SliceGeometry}, so
(unlike the legacy \code{ggslicer} this replaces) it works for oblique planes,
not just axis-aligned ones.
}
\examples{
\dontrun{
image <- ReadImage_fix("statistical_map.nii.gz")
mask <- ReadImage_fix("brainmask.nii.gz")
contours <- slice_contours(
  image, axis = "axial", coordinate = 0, levels = c(-2, 2),
  mask = mask, mask_fill = "nan"
)

library(ggplot2)
ggplot(contours, aes(x = x, y = y, group = interaction(level, obj))) +
  geom_path()
}
}
